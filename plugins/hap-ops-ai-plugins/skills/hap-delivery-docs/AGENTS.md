# hap-delivery-docs

> Codex / Claude Code 兼容入口。本目录是 `hap-delivery-docs` 技能。

Generate a complete HAP (Mingdao/明道云 超级应用平台) private-deployment delivery documentation suite。

完整说明见 `SKILL.md`；参考资料在 `data/`、`references/`、`assets/`（如有）。
